#include <stdio.h>

int main() {
int num;
int total = 0;

printf("Enter positive numbers to ADD (zero or negative number to STOP)\n");

while (1) {
printf("\nEnter a number: ");
scanf("%d", &num);

if (num <= 0) {
break;
}

total += num;
}

printf("Total sum is: %d\n", total);

return 0;
}
